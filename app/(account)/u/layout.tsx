import DashboardWrapper2 from "@/components/nav/dashboard-wrapper2";

export default function Layout({ children }: { children: React.ReactNode }) {
  return <DashboardWrapper2>{children}</DashboardWrapper2>;
}
